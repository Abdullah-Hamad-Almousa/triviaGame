# 🧠 Trivia Quiz Game

A command-line trivia quiz game

**Important**: This code **will not work** because the API URLs are placeholders (`"url to fetch the data"`).

**Before running**, you **must** replace the placeholder strings `"url to fetch the data"` in the code with these actual URLs.

---

## Features

- Browse and select from **20+ trivia categories** (e.g., Science, History, Video Games)
- Choose difficulty: **Easy**, **Medium**, or **Hard**
- Option for **random category**
- Multiple-choice questions with shuffled answers
- Real-time scoring and performance feedback
- HTML entity decoding (e.g., `&quot;` → `"`)
- Input validation and error handling

---

## Setup & Requirements

### Prerequisites
- This code build in python 3.12
- `requests` library
